import { ImageUrlInterceptor } from './image-url.interceptor';

describe('ImageUrlInterceptor', () => {
  it('should be defined', () => {
    expect(new ImageUrlInterceptor()).toBeDefined();
  });
});
